{
  "name": "plot-to-peak-jobber-integration",
  "version": "1.0.0",
  "private": true,
  "description": "Optional backend that connects Plot to Peak to the Jobber GraphQL API (OAuth 2.0) to create estimates/quotes and invoices and read payment status.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server-example.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
